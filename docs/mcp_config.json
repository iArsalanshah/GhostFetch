{
    "mcpServers": {
        "ghostfetch": {
            "command": "python",
            "args": [
                "-m",
                "ghostfetch.mcp_server"
            ],
            "env": {
                "SYNC_TIMEOUT_DEFAULT": "120",
                "MAX_SYNC_TIMEOUT": "300"
            }
        }
    }
}